import React from 'react';
import { Routes, Route } from 'react-router-dom';

const AuthRoutes = () => {
  return (
    <Routes>
      {/* Implementar rutas específicas de AuthRoutes */}
    </Routes>
  );
};

export default AuthRoutes;
