import React from 'react';
import { Routes, Route } from 'react-router-dom';

const PayrollRoutes = () => {
  return (
    <Routes>
      {/* Implementar rutas específicas de PayrollRoutes */}
    </Routes>
  );
};

export default PayrollRoutes;
